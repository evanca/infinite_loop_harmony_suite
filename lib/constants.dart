import 'package:flame/components.dart';
import 'package:flutter/material.dart';

class Constants {
  Constants._();

  static const binsImageSize = 1204 / 473;
  static const moodImageSize = 380 / 255;
  static const lastLevelNumber = 1000;
  static const gap = SizedBox(height: 10);
  static final gameSize = Vector2(720, 1600);
  static final desktopMainAreaSize = Vector2(2000, 1600);
  static const defaultUserName = 'Player';
}
